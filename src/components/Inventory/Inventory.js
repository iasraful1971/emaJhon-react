import React from 'react';

const Inventory = () => {
    return (
        <div style={{textAlign:'center'}}>
            <h2>the page's update is working</h2>
        </div>
    );
};

export default Inventory;